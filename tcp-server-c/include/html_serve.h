#ifndef HTML_SERVE_H
#define HTML_SERVE_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// Function to read an HTML file and return its contents
char* serve_html(const char* filename);

#endif
